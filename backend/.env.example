DATABASE_URL=postgresql://mibem:mibem@localhost:5432/mibem_qhse?schema=public
PORT=3000

# Secrets JWT — a generer et changer en production (ex: openssl rand -hex 32)
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret

# Dossier de stockage des photos uploadees (relatif au dossier backend)
UPLOAD_DIR=uploads
