DATABASE_URL="postgresql://qhse:qhse_dev_password@localhost:5432/qhse?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="15m"
REFRESH_SECRET="change-me-refresh-in-production"
PORT=3000
UPLOAD_DIR="./uploads"

# Notifications par e-mail (findings #25/#26/#40) — laisser SMTP_HOST vide
# désactive l'envoi (les notifications restent générées normalement).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="QHSE 360 <notifications@mibem.ci>"
