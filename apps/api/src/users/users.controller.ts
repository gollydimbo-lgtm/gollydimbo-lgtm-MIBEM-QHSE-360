import {Body,Controller,Delete,Get,Param,Patch,Post} from '@nestjs/common'; import {UsersService} from './users.service'; import {Roles} from '../common/roles.decorator'; import {RoleName} from '@prisma/client'; @Controller('users') export class UsersController{constructor(private s:UsersService){} @Get() list(){return this.s.list()} @Post() create(@Body()b:any){return this.s.create(b)} @Patch(':id/status') updateStatus(@Param('id')id:string,@Body()b:{status:'ACTIVE'|'INACTIVE'}){return this.s.updateStatus(id,b.status)} @Patch(':id/site') updateSite(@Param('id')id:string,@Body()b:{siteId:string|null}){return this.s.updateSite(id,b.siteId)} @Patch(':id/reset-password') resetPassword(@Param('id')id:string,@Body()b:{newPassword:string}){return this.s.resetPassword(id,b.newPassword)} @Delete(':id') @Roles(RoleName.ADMINISTRATEUR) remove(@Param('id')id:string){return this.s.remove(id)}}
