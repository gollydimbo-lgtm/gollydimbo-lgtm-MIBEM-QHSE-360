import {Controller,Get,Query} from '@nestjs/common'; import {PrismaService} from '../common/prisma.service'; @Controller('audit-logs') export class AuditController{constructor(private db:PrismaService){} @Get() list(@Query('module') module?:string){return this.db.auditLog.findMany({where:module?{module}:undefined,orderBy:{createdAt:'desc'},take:200,include:{user:{select:{id:true,email:true,firstName:true,lastName:true}}}})}}
