import {Body,Controller,Get,Post} from '@nestjs/common'; import {PrismaService} from '../common/prisma.service'; @Controller('documents') export class DocumentsController{constructor(private db:PrismaService){} @Get() list(){return this.db.document.findMany({include:{versions:true,attachments:true},orderBy:{updatedAt:'desc'}})} @Post() create(@Body() d:{code:string,title:string,category:string}){return this.db.document.create({data:d})}}
