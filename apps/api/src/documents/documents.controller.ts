import {Body,Controller,Get,Post,Query} from '@nestjs/common'; import {PrismaService} from '../common/prisma.service'; import {DocumentGroup} from '@prisma/client'; @Controller('documents') export class DocumentsController{constructor(private db:PrismaService){} @Get() list(@Query('group') group?:DocumentGroup){return this.db.document.findMany({where:group?{documentGroup:group}:undefined,include:{versions:true,attachments:true},orderBy:{updatedAt:'desc'}})} @Get('groups') groups(){return Object.values(DocumentGroup)} @Post() create(@Body() d:{code:string,title:string,category:string,documentGroup?:DocumentGroup}){return this.db.document.create({data:d})}}
