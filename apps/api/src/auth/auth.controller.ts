import { Body,Controller,Post } from '@nestjs/common'; import { AuthService } from './auth.service'; import { LoginDto } from './dto'; @Controller('auth') export class AuthController{constructor(private s:AuthService){} @Post('login') login(@Body() d:LoginDto){return this.s.login(d)} @Post('change-password') changePassword(@Body() b:{email:string;currentPassword:string;newPassword:string}){return this.s.changePassword(b.email,b.currentPassword,b.newPassword)}}
